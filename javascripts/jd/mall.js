
function messageListener(message){
    if(message.msg === ''){
        if(message.mode == 1){

        }
        else if(message.mode == 2){

        }
        else if(message.mode == 3){

        }
    }
}

function targetListener(mutation){
    console.log(mutation);
}
